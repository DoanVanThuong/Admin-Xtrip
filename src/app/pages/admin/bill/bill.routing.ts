import { Routes, RouterModule } from '@angular/router';
import { BillListComponent } from './billList/bill-list.component';

export const routes = [{ path: "", component: BillListComponent, pathMatch: "full" }];

